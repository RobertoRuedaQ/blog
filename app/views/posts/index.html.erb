<h2>index</h2>

<% @posts.each do |post| %>
<div class="post_wrapper">
	<h2 class="title"><%= link_to post.title, posts_path %></h2>
	<p class="date">
		<%= post.created_at.strftime("%B, %d, %Y") %>
	</p>
	<%end %>
	
</div>